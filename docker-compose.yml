version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./:/app:ro
    command: ["uvicorn", "main:api", "--host", "0.0.0.0", "--port", "3002"]
    